#include<iostream>
using namespace std;

int main(){
    string str;
    string temp = "";
    cout<<"Enter a string : ";
    getline(cin,str);
    for(int i = 0; i <= str.size(); i++){
        if((str[i]>='a' && str[i] <='z' ) || (str[i]))
    }

}